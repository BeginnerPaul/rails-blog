<h2><%= @article.title %></h2>

<p><%= @article.body%> </p>

<%= link_to "Edit article", edit_article_path %>

<%= link_to "Destroy article", article_path(@article), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
} %>

<h2>Comments:</h2>
<%= render @article.comments %>


<h2>Add Comment:</h2>

<%= render "comments/form", article: @article %>
